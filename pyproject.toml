[project]
name = "jepa-script"
version = "0.1.0"
description = "JEPA ml architecture for scripting language"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.9",
    "torch>=2.12.0",
]
